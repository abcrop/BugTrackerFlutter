part of app_helpers;

// this file focused on enum data

enum Responsive { mobile, tablet, desktop, none }

enum UserType {admin, developer, tester, endUser}

enum BugStatus {pending, ongoing, done, cancelled}

enum BugFlag {critical, major, medium, minor, trivial}

enum ActivityType {postedNewBug, changesBugStatus, }

